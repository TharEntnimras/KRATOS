import 'package:flutter/material.dart';
 
class ForoScreen extends StatelessWidget {
  ForoScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Text('Estas en el Foro'),
    );
  }
}